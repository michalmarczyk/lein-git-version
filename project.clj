(defproject lein-git-version "0.0.1-SNAPSHOT"
  :description "Use git describe --tags to determine the name of (über)jars."
  :dev-dependencies [[org.clojure/clojure "[1.2.0,1.3.0]"]]
  :eval-in-leiningen true)
